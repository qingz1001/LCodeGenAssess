#include <stdio.h>

int main() {
    int n, q;
    scanf("%d%d", &n, &q);
    
    while (q--) {
        int op, l, r;
        scanf("%d%d%d", &op, &l, &r);
        
        if (op == 1) {
            int a, b;
            scanf("%d%d", &a, &b);
            // 这里应该是处理第一种操作的代码
        } else if (op == 2) {
            // 这里应该是处理第二种操作的代码
        }
    }
    
    return 0;
}